{
  "swagger": "2.0",
  "info": {
    "title": "Session Service",
    "description": "This API is intended to manage sessions in a ZITADEL instance. Follow the guides on how to [build your own Login UI](/docs/guides/integrate/login-ui) and learn how to use the Session API.",
    "version": "2.0",
    "contact": {
      "name": "ZITADEL",
      "url": "https://zitadel.com",
      "email": "hi@zitadel.com"
    },
    "license": {
      "name": "Apache 2.0",
      "url": "https://github.com/zitadel/zitadel/blob/main/LICENSING.md"
    }
  },
  "tags": [
    {
      "name": "SessionService"
    }
  ],
  "host": "$CUSTOM-DOMAIN",
  "basePath": "/",
  "schemes": [
    "https",
    "http"
  ],
  "consumes": [
    "application/json",
    "application/grpc",
    "application/grpc-web+proto"
  ],
  "produces": [
    "application/json",
    "application/grpc",
    "application/grpc-web+proto"
  ],
  "paths": {
    "/v2/sessions": {
      "post": {
        "summary": "Create Session",
        "description": "Create a new session with initial checks, metadata and challenges for further verification.\nA token will be returned, which is required for using the session as authentication, e.g.\nwhen authenticating an OIDC auth request or SAML request.\nAdditionally, the session token can be used as OAuth2 access token to authenticate against\nthe ZITADEL APIs.\n\nRequired permissions:\n  - `session.write`",
        "operationId": "SessionService_CreateSession",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/v2CreateSessionResponse"
            }
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v2CreateSessionRequest"
            }
          }
        ],
        "tags": [
          "SessionService"
        ]
      }
    },
    "/v2/sessions/search": {
      "post": {
        "summary": "List sessions",
        "description": "Searches for sessions matching the given query. You can search by session ID, user ID,\ncreation date, creator, user agent or expiration date.\n\nRequired permissions:\n  - `session.read`\n  - no permission required to search for own sessions",
        "operationId": "SessionService_ListSessions",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/v2ListSessionsResponse"
            }
          },
          "400": {
            "description": "invalid list query",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v2ListSessionsRequest"
            }
          }
        ],
        "tags": [
          "SessionService"
        ]
      }
    },
    "/v2/sessions/{sessionId}": {
      "get": {
        "summary": "Get Session",
        "description": "Retrieve a session by its ID. Returns all information about the session, including\nthe factors that were verified, the metadata, user agent information and possible expiration date.\nThe session token is required unless either of the following conditions is met:\n  - the caller created the session\n  - the authenticated user requests their own session (checked user)\n  - the security token provided in the authorization header has the same user agent as the session\n  - the caller is granted the permission session.read permission on either the instance or on the checked user's organization\n\nRequired permissions:\n  - `session.read`\n  - no permission required to get own sessions (see above) or when providing the current session token",
        "operationId": "SessionService_GetSession",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/v2GetSessionResponse"
            }
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "sessionId",
            "description": "The unique identifier of the session to be retrieved.",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "sessionToken",
            "description": "The current token of the session, previously returned on the create / update request.\nThe token is required unless either of the following conditions is met:\n- the caller created the session\n- the authenticated user requests their own session (checked user)\n- the security token provided in the authorization header has the same user agent as the session\n- the caller is granted the permission session.read permission on either the instance or on the checked user's organization",
            "in": "query",
            "required": false,
            "type": "string"
          }
        ],
        "tags": [
          "SessionService"
        ]
      },
      "delete": {
        "summary": "DeleteSession",
        "description": "Terminate an existing session. This invalidates the session and its token.\nThe session can no longer be used for the authentication of other resources\nor to authenticate against the ZITADEL APIs.\n\nYou can only terminate your own session, unless you are granted the `session.delete` permission.\n\nRequired permissions:\n  - `session.delete`\n  - no permission required for own sessions or when providing the current session token",
        "operationId": "SessionService_DeleteSession",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/v2DeleteSessionResponse"
            }
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "sessionId",
            "description": "The unique identifier of the session to be terminated.",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/SessionServiceDeleteSessionBody"
            }
          }
        ],
        "tags": [
          "SessionService"
        ]
      },
      "patch": {
        "summary": "Set Session",
        "description": "Update an existing session with new information like additional checks or metadata\nor request additional challenges.\nA new session token will be returned. Note that the previous token will be invalidated.\n\nRequired permissions:\n  - `session.write`",
        "operationId": "SessionService_SetSession",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/v2SetSessionResponse"
            }
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "sessionId",
            "description": "The unique identifier of the session to be updated.",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/SessionServiceSetSessionBody"
            }
          }
        ],
        "tags": [
          "SessionService"
        ]
      }
    }
  },
  "definitions": {
    "OTPEmailReturnCode": {
      "type": "object"
    },
    "OTPEmailSendCode": {
      "type": "object",
      "properties": {
        "urlTemplate": {
          "type": "string",
          "example": "https://example.com/otp/verify?userID={{.UserID}}\u0026code={{.Code}}",
          "description": "Optionally set a url_template, which will be used in the mail sent by ZITADEL to guide the user to your verification page.\nIf no template is set, the default ZITADEL url will be used.\n\nThe following placeholders can be used: Code, UserID, LoginName, DisplayName, PreferredLanguage, SessionID",
          "maxLength": 200,
          "minLength": 1
        }
      }
    },
    "RequestChallengesOTPEmail": {
      "type": "object",
      "properties": {
        "sendCode": {
          "$ref": "#/definitions/OTPEmailSendCode"
        },
        "returnCode": {
          "$ref": "#/definitions/OTPEmailReturnCode"
        }
      }
    },
    "RequestChallengesOTPSMS": {
      "type": "object",
      "properties": {
        "returnCode": {
          "type": "boolean",
          "description": "Request the code to be returned instead of sending an SMS.\nThis is useful for testing or in case you want to send the code yourself."
        }
      }
    },
    "SessionServiceDeleteSessionBody": {
      "type": "object",
      "properties": {
        "sessionToken": {
          "type": "string",
          "title": "The current token of the session, previously returned on the create / update request.\nThe token is required unless either of the following conditions is met:\n- the caller created the session\n- the authenticated user requests their own session (checked user)\n- the security token provided in the authorization header has the same user agent as the session\n- the caller is granted the permission session.delete permission on either the instance or on the checked user's organization"
        }
      }
    },
    "SessionServiceSetSessionBody": {
      "type": "object",
      "properties": {
        "sessionToken": {
          "type": "string",
          "description": "Deprecated: the session token is no longer required when updating a session\nand will be ignored when provided."
        },
        "checks": {
          "$ref": "#/definitions/v2Checks",
          "description": "Additional checks to be performed on the session.\nSuccessful checks will be stated as factors on the session.\nRe-executing a check that was already successful will overwrite the previous check time.\nFor example, if a password check was already successful, it can be re-checked to update the check time.\nNote that a user check can only be performed once per session and cannot be changed afterwards.\nSome checks require that the user is already checked, either in the previous or the same request.\nFor example, a password check requires that the user is already checked.\nOther checks, like WebAuthN or IDP intent, require that the user is already checked and a challenge\nwas requested in any previous request.\nOn successful checks, the session's `factors` field will be updated."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "format": "byte"
          },
          "description": "Additional custom key value pairs to be stored on the session.\nExisting keys will be overwritten. To delete a key, set its value to an empty byte array.\nNote that metadata keys cannot be changed once the session has been created.\nYou need to create a new entry and delete the old one instead."
        },
        "challenges": {
          "$ref": "#/definitions/v2RequestChallenges",
          "description": "Additional challenges to be requested for further verification.\nThe challenges will be created and returned in the response.\nThe challenges can then be used for further checks with the following SetSession method.\nRequesting a challenge that was already requested will overwrite the previous challenge."
        },
        "lifetime": {
          "type": "string",
          "example": "18000s",
          "description": "Update the duration after which the session will be automatically invalidated.\nIf not set, the lifetime will not be changed.\nNote that an expired session cannot be used for authentication anymore, but will still be listed\nuntil it is deleted."
        }
      }
    },
    "UserAgentHeaderValues": {
      "type": "object",
      "properties": {
        "values": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "description": "A header may have multiple values.\nIn Go, headers are defined\nas map[string][]string, but protobuf\ndoesn't allow this scheme."
    },
    "objectv2ListDetails": {
      "type": "object",
      "properties": {
        "totalResult": {
          "type": "string",
          "format": "uint64",
          "example": "2"
        },
        "processedSequence": {
          "type": "string",
          "format": "uint64",
          "example": "267831"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "the last time the projection got updated"
        }
      }
    },
    "objectv2ListQuery": {
      "type": "object",
      "properties": {
        "offset": {
          "type": "string",
          "format": "uint64",
          "example": "0"
        },
        "limit": {
          "type": "integer",
          "format": "int64",
          "example": 100,
          "description": "Maximum amount of events returned. The default is set to 1000 in https://github.com/zitadel/zitadel/blob/new-eventstore/cmd/zitadel/startup.yaml. If the limit exceeds the maximum configured ZITADEL will throw an error. If no limit is present the default is taken."
        },
        "asc": {
          "type": "boolean",
          "description": "default is descending"
        }
      },
      "description": "Object unspecific list filters like offset, limit and asc/desc.",
      "title": "General List Query"
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "protobufNullValue": {
      "type": "string",
      "enum": [
        "NULL_VALUE"
      ],
      "default": "NULL_VALUE",
      "description": "`NullValue` is a singleton enumeration to represent the null value for the\n`Value` type union.\n\nThe JSON representation for `NullValue` is JSON `null`.\n\n - NULL_VALUE: Null value."
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    },
    "v2Challenges": {
      "type": "object",
      "properties": {
        "webAuthN": {
          "$ref": "#/definitions/v2ChallengesWebAuthN"
        },
        "otpSms": {
          "type": "string"
        },
        "otpEmail": {
          "type": "string"
        }
      }
    },
    "v2ChallengesWebAuthN": {
      "type": "object",
      "properties": {
        "publicKeyCredentialRequestOptions": {
          "type": "object",
          "example": {
            "publicKey": {
              "allowCredentials": [
                {
                  "id": "ATmqBg-99qyOZk2zloPdJQyS2R7IkFT7v9Hoos_B_nM",
                  "type": "public-key"
                }
              ],
              "challenge": "GAOHYz2jE69kJMYo6Laij8yWw9-dKKgbViNhfuy0StA",
              "rpId": "localhost",
              "timeout": 300000,
              "userVerification": "required"
            }
          }
        }
      },
      "title": "WebAuthN contains the options for the Assertion Generation (dictionary PublicKeyCredentialRequestOptions).\nGenerated helper methods transform the field to JSON, for use in a WebauthN client.\nSee also: https://www.w3.org/TR/webauthn/#dictdef-publickeycredentialrequestoptions"
    },
    "v2CheckIDPIntent": {
      "type": "object",
      "properties": {
        "idpIntentId": {
          "type": "string",
          "example": "d654e6ba-70a3-48ef-a95d-37c8d8a7901a",
          "description": "The ID of the idp intent, previously returned on the success response of the IDP callback.",
          "maxLength": 200,
          "minLength": 1
        },
        "idpIntentToken": {
          "type": "string",
          "example": "SJKL3ioIDpo342ioqw98fjp3sdf32wahb=",
          "description": "The token of the idp intent, previously returned on the success response of the IDP callback.",
          "maxLength": 200,
          "minLength": 1
        }
      },
      "required": [
        "idpIntentId",
        "idpIntentToken"
      ]
    },
    "v2CheckOTP": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "example": "3237642",
          "description": "The One-Time Password sent over SMS or Email of the user to be checked.",
          "minLength": 1
        }
      },
      "required": [
        "code"
      ]
    },
    "v2CheckPassword": {
      "type": "object",
      "properties": {
        "password": {
          "type": "string",
          "example": "V3ryS3cure!",
          "description": "The password of the user to be checked.",
          "maxLength": 200,
          "minLength": 1
        }
      },
      "required": [
        "password"
      ]
    },
    "v2CheckTOTP": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "example": "323764",
          "description": "The Time-based One-Time Password generated by the user's TOTP authenticator app.",
          "maxLength": 6,
          "minLength": 6
        }
      },
      "required": [
        "code"
      ]
    },
    "v2CheckUser": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "example": "d654e6ba-70a3-48ef-a95d-37c8d8a7901a",
          "description": "The unique identifier of the user to be checked.",
          "maxLength": 200,
          "minLength": 1
        },
        "loginName": {
          "type": "string",
          "example": "mini@mouse.com",
          "description": "The login name of the user to be checked. It will search case insensitive.\nNote this only checks for the computed login name and not for any organization scoped usernames.\nAlso note that it will not check for emails or phone numbers, even if the corresponding\nsetting is enabled. Use the user service ListUsers method to find a user by email or phone number first\nto obtain the user ID or login name.",
          "maxLength": 200,
          "minLength": 1
        }
      },
      "required": [
        "userId",
        "loginName"
      ]
    },
    "v2CheckWebAuthN": {
      "type": "object",
      "properties": {
        "credentialAssertionData": {
          "type": "object",
          "description": "The JSON representation of the public key credential issued by the WebAuthN client.",
          "maxLength": 1048576,
          "minLength": 55
        }
      },
      "required": [
        "credentialAssertionData"
      ]
    },
    "v2Checks": {
      "type": "object",
      "properties": {
        "user": {
          "$ref": "#/definitions/v2CheckUser",
          "description": "Check the user by its user ID or login name.\nA user check can only be performed once per session and cannot be changed afterwards.\nOn successful user check, the session's `factors` field will be updated with a `user` factor\ncontaining the checked user's ID, login name, display name, organization ID and verification time.\nNote that some other checks require that the user is already checked, either in the previous\nor the same request. For example, a password check requires that the user is already checked."
        },
        "password": {
          "$ref": "#/definitions/v2CheckPassword",
          "description": "Check the password and update the session on success.\nRequires that the user is already checked, either in the previous or the same request.\nOn successful password check, the session's `factors` field will be updated with a `password` factor,\ncontaining the verification time.\nOn failed password check id: \"COMMAND-3M0fs\" wll be returned.\nOn user locked out id: \"COMMAND-JLK35\"/\"COMMAND-SFA3t\" will be returned."
        },
        "webAuthN": {
          "$ref": "#/definitions/v2CheckWebAuthN",
          "description": "Check the public key credential issued by the WebAuthN client.\nRequires that the user is already checked and a WebAuthN challenge to be requested,\nin any previous request.\nOn successful WebAuthN check, the session's `factors` field will be updated with a `webauthn` factor,\ncontaining the verification time and if the user presence was verified."
        },
        "idpIntent": {
          "$ref": "#/definitions/v2CheckIDPIntent",
          "description": "Check the IDP intent and update the session on success.\nRequires that the user is already checked and an IDP intent succeeded and has not expired yet.\nNote that the IDP intent must be either matching the same user as checked in the session,\nor there must not be an external IDP linked to the intent's user.\nOn successful IDP intent check, the session's `factors` field will be updated with an `intent` factor,\ncontaining the verification time."
        },
        "totp": {
          "$ref": "#/definitions/v2CheckTOTP",
          "description": "Check the Time-based One-Time Password and update the session on success.\nRequires that the user is already checked, either in the previous or the same request.\nOn successful TOTP check, the session's `factors` field will be updated with a `totp` factor,\ncontaining the verification time."
        },
        "otpSms": {
          "$ref": "#/definitions/v2CheckOTP",
          "description": "Check the One-Time Password sent over SMS and update the session on success.\nRequires that the user is already checked, either in the previous or the same request.\nOn successful OTP SMS check, the session's `factors` field will be updated with an `otp` factor,\ncontaining the verification time.\nNote that the OTP code is valid for a single use only and will be invalidated after a successful check."
        },
        "otpEmail": {
          "$ref": "#/definitions/v2CheckOTP",
          "description": "Check the One-Time Password sent over Email and update the session on success.\nRequires that the user is already checked, either in the previous or the same request.\nOn successful OTP Email check, the session's `factors` field will be updated with an `otp` factor,\ncontaining the verification time.\nNote that the OTP code is valid for a single use only and will be invalidated after a successful check."
        }
      }
    },
    "v2CreateSessionRequest": {
      "type": "object",
      "properties": {
        "checks": {
          "$ref": "#/definitions/v2Checks",
          "description": "The checks to be performed during session creation.\nNote that a user check can only be performed once per session and cannot be changed afterwards.\nSome checks require that the user is already checked, either in the previous or the same\nrequest. For example, a password check requires that the user is already checked.\nOther checks, like WebAuthN or IDP intent, require that the user is already checked and a challenge\nwas requested in any previous request.\nOn successful checks, the session's `factors` field will be updated."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "format": "byte"
          },
          "description": "Custom key value list to be stored on the session."
        },
        "challenges": {
          "$ref": "#/definitions/v2RequestChallenges",
          "description": "Challenges to be requested for further verification.\nThe challenges will be created and returned in the response.\nThe challenges can then be used for further checks with the following SetSession method."
        },
        "userAgent": {
          "$ref": "#/definitions/v2UserAgent",
          "description": "Store information about the user agent of the client.\nThis information is stored on the session and can be used for auditing or security purposes.\nSessions can also be filtered by user agent information."
        },
        "lifetime": {
          "type": "string",
          "example": "18000s",
          "description": "Duration after which the session will be automatically invalidated.\nIf not set, the session will not expire automatically and must be deleted manually.\nNote that an expired session cannot be used for authentication anymore, but will still be listed\nuntil it is deleted."
        }
      }
    },
    "v2CreateSessionResponse": {
      "type": "object",
      "properties": {
        "details": {
          "$ref": "#/definitions/v2Details"
        },
        "sessionId": {
          "type": "string",
          "example": "222430354126975533",
          "description": "Unique identifier of the session."
        },
        "sessionToken": {
          "type": "string",
          "description": "The current token of the session, which is required for using the session as authentication,\ne.g.when authenticating an OIDC auth request or SAML request.\nAdditionally, the session token can be used as OAuth2 access token to authenticate against\nthe ZITADEL APIs."
        },
        "challenges": {
          "$ref": "#/definitions/v2Challenges",
          "description": "The challenges that were requested for the session.\nThe challenges can be used for further checks with the SetSession method."
        }
      }
    },
    "v2CreationDateQuery": {
      "type": "object",
      "properties": {
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "description": "The creation date to compare the session's creation date against."
        },
        "method": {
          "$ref": "#/definitions/zitadelv1TimestampQueryMethod",
          "description": "The method of comparison to use for the creation date.\nThis defines whether to search for sessions created before, after, or exactly at the specified date."
        }
      }
    },
    "v2CreatorQuery": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "example": "69629023906488334",
          "description": "ID of the user who created the session. If empty, the calling user's ID is used.",
          "maxLength": 200
        }
      }
    },
    "v2DeleteSessionResponse": {
      "type": "object",
      "properties": {
        "details": {
          "$ref": "#/definitions/v2Details"
        }
      }
    },
    "v2Details": {
      "type": "object",
      "properties": {
        "sequence": {
          "type": "string",
          "format": "uint64",
          "example": "2",
          "description": "on read: the sequence of the last event reduced by the projection\n\non manipulation: the timestamp of the event(s) added by the manipulation",
          "title": "sequence represents the order of events. It's always counting"
        },
        "changeDate": {
          "type": "string",
          "format": "date-time",
          "description": "on read: the timestamp of the last event reduced by the projection\n\non manipulation: the timestamp of the event(s) added by the manipulation",
          "title": "change_date is the timestamp when the object was changed"
        },
        "resourceOwner": {
          "type": "string",
          "example": "69629023906488334",
          "title": "resource_owner is the organization or instance_id an object belongs to"
        },
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "title": "creation_date is the timestamp where the first operation on the object was made"
        }
      }
    },
    "v2ExpirationDateQuery": {
      "type": "object",
      "properties": {
        "expirationDate": {
          "type": "string",
          "format": "date-time",
          "description": "The expiration date to compare the session's expiration date against."
        },
        "method": {
          "$ref": "#/definitions/zitadelv1TimestampQueryMethod",
          "description": "The method of comparison to use for the expiration date.\nThis defines whether to search for sessions expiring before, after, or exactly at the specified date."
        }
      }
    },
    "v2Factors": {
      "type": "object",
      "properties": {
        "user": {
          "$ref": "#/definitions/v2UserFactor"
        },
        "password": {
          "$ref": "#/definitions/v2PasswordFactor"
        },
        "webAuthN": {
          "$ref": "#/definitions/v2WebAuthNFactor"
        },
        "intent": {
          "$ref": "#/definitions/v2IntentFactor"
        },
        "totp": {
          "$ref": "#/definitions/v2TOTPFactor"
        },
        "otpSms": {
          "$ref": "#/definitions/v2OTPFactor"
        },
        "otpEmail": {
          "$ref": "#/definitions/v2OTPFactor"
        }
      }
    },
    "v2GetSessionResponse": {
      "type": "object",
      "properties": {
        "session": {
          "$ref": "#/definitions/v2Session"
        }
      }
    },
    "v2IDsQuery": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of session IDs to search for.\nIf multiple IDs are provided, sessions matching any of the IDs will be returned."
        }
      }
    },
    "v2IntentFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the intent was last verified."
        }
      }
    },
    "v2ListSessionsRequest": {
      "type": "object",
      "properties": {
        "query": {
          "$ref": "#/definitions/objectv2ListQuery",
          "description": "List limitations and ordering."
        },
        "queries": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2SearchQuery"
          },
          "description": "The criteria to be used when searching for sessions.\nMultiple queries will be combined with a logical AND."
        },
        "sortingColumn": {
          "$ref": "#/definitions/v2SessionFieldName",
          "description": "The column to be used for sorting the sessions."
        }
      }
    },
    "v2ListSessionsResponse": {
      "type": "object",
      "properties": {
        "details": {
          "$ref": "#/definitions/objectv2ListDetails",
          "description": "Contains details about the response, such as the total number of sessions."
        },
        "sessions": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2Session"
          },
          "description": "The sessions matching the search query. There might be more sessions available\nthan returned in this response. Use the details field to see if there are more sessions\navailable and to get the total count of sessions matching the query."
        }
      }
    },
    "v2OTPFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the One-Time Password was last verified either by SMS or Email."
        }
      }
    },
    "v2PasswordFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the password was last verified."
        }
      }
    },
    "v2RequestChallenges": {
      "type": "object",
      "properties": {
        "webAuthN": {
          "$ref": "#/definitions/v2RequestChallengesWebAuthN",
          "description": "WebAuthN requests a challenge to be used in the WebAuthN authentication ceremony.\nThey can be used for both passkey and U2F authentication.\nThey're required for a webauthn check at the SetSession endpoint."
        },
        "otpSms": {
          "$ref": "#/definitions/RequestChallengesOTPSMS",
          "description": "OTPSMS requests a code to be sent via SMS to the user's primary phone.\nIt is required for an OTP check at the SetSession endpoint."
        },
        "otpEmail": {
          "$ref": "#/definitions/RequestChallengesOTPEmail",
          "description": "OTPEmail requests a code to be sent via email to the user's primary email address.\nIt is required for an OTP check at the SetSession endpoint."
        }
      }
    },
    "v2RequestChallengesWebAuthN": {
      "type": "object",
      "properties": {
        "domain": {
          "type": "string",
          "title": "The domain on which the session was created. Will be used in the WebAuthN challenge.\nIt must be either the exact domain or a top-level domain of the origin of the request.\nFor example if the request is coming from \"login.example.com\", the domain can be\n\"login.example.com\" or \"example.com\", but not \"other.com\" or \"sub.login.example.com\".\nSee also: https://www.w3.org/TR/webauthn/#relying-party-identifier"
        },
        "userVerificationRequirement": {
          "$ref": "#/definitions/v2UserVerificationRequirement",
          "description": "User verification that is required during validation.\nWhen set to `USER_VERIFICATION_REQUIREMENT_REQUIRED` the behaviour is for passkey authentication.\nOther values will mean U2F.\nSee also: https://www.w3.org/TR/webauthn/#enum-userVerificationRequirement\nIf either the value is set to USER_VERIFICATION_REQUIREMENT_REQUIRED or the user verification\nis passed as part of the authentication ceremony, the user_verified flag will be set in the resulting webauthn factor."
        }
      },
      "required": [
        "domain",
        "userVerificationRequirement"
      ]
    },
    "v2SearchQuery": {
      "type": "object",
      "properties": {
        "idsQuery": {
          "$ref": "#/definitions/v2IDsQuery",
          "description": "Search for sessions with the given IDs."
        },
        "userIdQuery": {
          "$ref": "#/definitions/v2UserIDQuery",
          "description": "Search for sessions of the given user ID."
        },
        "creationDateQuery": {
          "$ref": "#/definitions/v2CreationDateQuery",
          "description": "Search for sessions based on their creation date.\nThis can be used to find sessions created before or after a certain date."
        },
        "creatorQuery": {
          "$ref": "#/definitions/v2CreatorQuery",
          "description": "Search for sessions created by a specific user."
        },
        "userAgentQuery": {
          "$ref": "#/definitions/v2UserAgentQuery",
          "description": "Search for sessions based on the user agent used to create the session.\nIt can be used to find sessions created from a specific device or browser.\nThis is typically done for providing a list of active sessions to the user\nin a user interface."
        },
        "expirationDateQuery": {
          "$ref": "#/definitions/v2ExpirationDateQuery",
          "description": "Search for sessions based on their expiration date.\nThis can be used to find sessions that are set to expire before or after a certain date."
        }
      }
    },
    "v2Session": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the session."
        },
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp the session was created."
        },
        "changeDate": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp the session was last updated."
        },
        "sequence": {
          "type": "string",
          "format": "uint64",
          "description": "The sequence of the session represents the change sequence of the session."
        },
        "factors": {
          "$ref": "#/definitions/v2Factors",
          "description": "The factors that have been checked for this session, e.g. the user, password and more.\nIf a factor is set, it means it has been checked and is valid.\nIf a factor is not set, it means it has not been checked or is invalid.\nIf a factor is set, the verified_at timestamp indicates when it was last checked."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "format": "byte"
          },
          "description": "Metadata contains custom key value pairs set by the user.\nThe metadata is not interpreted by ZITADEL and can be used to store any information\nrelevant to the session."
        },
        "userAgent": {
          "$ref": "#/definitions/v2UserAgent",
          "description": "UserAgent contains information about the user agent used to create the session.\nThis can include information such as the IP address, browser type, and operating system and\na fingerprint id.\nThis can be used to identify and filter sessions based on the user agent."
        },
        "expirationDate": {
          "type": "string",
          "format": "date-time",
          "description": "ExpirationDate is the time the session will be automatically invalidated.\nIf not set, the session does not expire automatically."
        }
      }
    },
    "v2SessionFieldName": {
      "type": "string",
      "enum": [
        "SESSION_FIELD_NAME_UNSPECIFIED",
        "SESSION_FIELD_NAME_CREATION_DATE"
      ],
      "default": "SESSION_FIELD_NAME_UNSPECIFIED"
    },
    "v2SetSessionResponse": {
      "type": "object",
      "properties": {
        "details": {
          "$ref": "#/definitions/v2Details"
        },
        "sessionToken": {
          "type": "string",
          "description": "The current token of the session, which is required for using the session as authentication,\ne.g.when authenticating an OIDC auth request or SAML request.\nAdditionally, the session token can be used as OAuth2 access token to authenticate against\nthe ZITADEL APIs.\nThe previous token was invalidated and can no longer be used."
        },
        "challenges": {
          "$ref": "#/definitions/v2Challenges",
          "description": "The challenges that were requested for the session.\nThe challenges can be used for further checks with the SetSession method."
        }
      }
    },
    "v2TOTPFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the Time-based One-Time Password was last verified."
        }
      }
    },
    "v2UserAgent": {
      "type": "object",
      "properties": {
        "fingerprintId": {
          "type": "string",
          "description": "FingerprintID is a unique identifier for the user agent's fingerprint.\nIt can be used to group sessions by device or browser."
        },
        "ip": {
          "type": "string",
          "description": "IP is the IP address from which the session was created."
        },
        "description": {
          "type": "string",
          "description": "Description is a human-readable description of the user agent."
        },
        "header": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/UserAgentHeaderValues"
          }
        }
      }
    },
    "v2UserAgentQuery": {
      "type": "object",
      "properties": {
        "fingerprintId": {
          "type": "string",
          "example": "69629023906488334",
          "description": "Finger print id of the user agent used for the session.\nSet an empty fingerprint_id to use the user agent from the call.\nIf the user agent is not available from the current token, an error will be returned.",
          "maxLength": 200
        }
      }
    },
    "v2UserFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user was last verified."
        },
        "id": {
          "type": "string",
          "description": "The unique identifier of the user that was verified."
        },
        "loginName": {
          "type": "string",
          "description": "The login name of the user that was verified."
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user that was verified."
        },
        "organizationId": {
          "type": "string",
          "description": "The id of the organization the user belongs to."
        }
      }
    },
    "v2UserIDQuery": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "ID of the user whose sessions are being searched for."
        }
      }
    },
    "v2UserVerificationRequirement": {
      "type": "string",
      "enum": [
        "USER_VERIFICATION_REQUIREMENT_UNSPECIFIED",
        "USER_VERIFICATION_REQUIREMENT_REQUIRED",
        "USER_VERIFICATION_REQUIREMENT_PREFERRED",
        "USER_VERIFICATION_REQUIREMENT_DISCOURAGED"
      ],
      "default": "USER_VERIFICATION_REQUIREMENT_UNSPECIFIED"
    },
    "v2WebAuthNFactor": {
      "type": "object",
      "properties": {
        "verifiedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the passkey challenge was last verified."
        },
        "userVerified": {
          "type": "boolean",
          "description": "Indicates if the user presence was verified during the last challenge.\nThis can be used to determine if the factor can be considered as multi-factor authentication."
        }
      }
    },
    "zitadelv1TimestampQueryMethod": {
      "type": "string",
      "enum": [
        "TIMESTAMP_QUERY_METHOD_EQUALS",
        "TIMESTAMP_QUERY_METHOD_GREATER",
        "TIMESTAMP_QUERY_METHOD_GREATER_OR_EQUALS",
        "TIMESTAMP_QUERY_METHOD_LESS",
        "TIMESTAMP_QUERY_METHOD_LESS_OR_EQUALS"
      ],
      "default": "TIMESTAMP_QUERY_METHOD_EQUALS"
    }
  },
  "securityDefinitions": {
    "OAuth2": {
      "type": "oauth2",
      "flow": "accessCode",
      "authorizationUrl": "$CUSTOM-DOMAIN/oauth/v2/authorize",
      "tokenUrl": "$CUSTOM-DOMAIN/oauth/v2/token",
      "scopes": {
        "openid": "openid",
        "urn:zitadel:iam:org:project:id:zitadel:aud": "urn:zitadel:iam:org:project:id:zitadel:aud"
      }
    }
  },
  "security": [
    {
      "OAuth2": [
        "openid",
        "urn:zitadel:iam:org:project:id:zitadel:aud"
      ]
    }
  ],
  "externalDocs": {
    "description": "Detailed information about ZITADEL",
    "url": "https://zitadel.com/docs"
  }
}
