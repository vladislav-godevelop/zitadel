{
  "swagger": "2.0",
  "info": {
    "title": "zitadel/instance/v2/instance_service.proto",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "InstanceService"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {},
  "definitions": {
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    },
    "v2AddCustomDomainResponse": {
      "type": "object",
      "properties": {
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "CreationDate is the timestamp when the domain was added."
        }
      }
    },
    "v2AddTrustedDomainResponse": {
      "type": "object",
      "properties": {
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "CreationDate is the timestamp when the trusted domain was added."
        }
      }
    },
    "v2CustomDomain": {
      "type": "object",
      "properties": {
        "instanceId": {
          "type": "string",
          "description": "InstanceID is the unique identifier of the instance the domain belongs to."
        },
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "CreationDate is the timestamp when the domain was created."
        },
        "domain": {
          "type": "string",
          "example": "zitadel.com",
          "description": "Domain is the fully qualified domain name.\nIt must be unique across all instances in the system."
        },
        "primary": {
          "type": "boolean",
          "description": "Primary states whether this domain is the primary domain of the instance.\nEach instance must have exactly one primary domain.\nThe primary domain is used for various purposes and acts as fallback\nin those cases, e.g if no explicit domain is specified."
        },
        "generated": {
          "type": "boolean",
          "description": "Generate states whether this domain was auto-generated by the system.\nAuto-generated domains follow a specific pattern and are created\nwhen a new instance is created.\nThey cannot be deleted, but the primary domain can be changed\nto a manually added domain."
        }
      }
    },
    "v2CustomDomainFilter": {
      "type": "object",
      "properties": {
        "domainFilter": {
          "$ref": "#/definitions/v2DomainFilter",
          "description": "Filter for a specific custom domain."
        },
        "generatedFilter": {
          "type": "boolean",
          "description": "Filter whether the domain is auto-generated."
        },
        "primaryFilter": {
          "type": "boolean",
          "description": "Filter whether the domain is the primary domain of the instance."
        }
      }
    },
    "v2CustomDomainsFilter": {
      "type": "object",
      "properties": {
        "domains": {
          "type": "array",
          "example": [
            "my-instace.zitadel.cloud",
            "auth.custom.com"
          ],
          "items": {
            "type": "string"
          },
          "description": "The domains to query for. All instances that have at least one of the\nspecified domains will be returned.\nA maximum of 20 domains can be specified.",
          "maxItems": 20
        }
      }
    },
    "v2DeleteInstanceResponse": {
      "type": "object",
      "properties": {
        "deletionDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "DeletionDate is the timestamp when the instance was deleted."
        }
      }
    },
    "v2DomainFieldName": {
      "type": "string",
      "enum": [
        "DOMAIN_FIELD_NAME_UNSPECIFIED",
        "DOMAIN_FIELD_NAME_DOMAIN",
        "DOMAIN_FIELD_NAME_PRIMARY",
        "DOMAIN_FIELD_NAME_GENERATED",
        "DOMAIN_FIELD_NAME_CREATION_DATE"
      ],
      "default": "DOMAIN_FIELD_NAME_UNSPECIFIED"
    },
    "v2DomainFilter": {
      "type": "object",
      "properties": {
        "domain": {
          "type": "string",
          "example": "zitadel.com",
          "description": "The domain to filter for.",
          "maxLength": 253
        },
        "method": {
          "$ref": "#/definitions/v2TextQueryMethod",
          "description": "The method to use for text comparison.\nIf not specified, EQUALS is used."
        }
      }
    },
    "v2FieldName": {
      "type": "string",
      "enum": [
        "FIELD_NAME_UNSPECIFIED",
        "FIELD_NAME_ID",
        "FIELD_NAME_NAME",
        "FIELD_NAME_CREATION_DATE"
      ],
      "default": "FIELD_NAME_UNSPECIFIED"
    },
    "v2Filter": {
      "type": "object",
      "properties": {
        "inIdsFilter": {
          "$ref": "#/definitions/v2InIDsFilter",
          "description": "Filter for one or more specific instance IDs."
        },
        "customDomainsFilter": {
          "$ref": "#/definitions/v2CustomDomainsFilter",
          "description": "Filter for instances that have at least one of the specified custom domains."
        }
      }
    },
    "v2GetInstanceResponse": {
      "type": "object",
      "properties": {
        "instance": {
          "$ref": "#/definitions/v2Instance",
          "title": "The instance matching the instance ID"
        }
      }
    },
    "v2InIDsFilter": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "example": [
            "69629023906488334",
            "69622366012355662"
          ],
          "items": {
            "type": "string",
            "maxLength": 200,
            "minLength": 1
          },
          "description": "Defines the ids to query for."
        }
      }
    },
    "v2Instance": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "example": "69629023906488334",
          "description": "ID is the unique identifier of the instance."
        },
        "changeDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "ChangeDate is the timestamp when the instance was last changed."
        },
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "CreationDate is the timestamp when the instance was created."
        },
        "state": {
          "$ref": "#/definitions/v2State",
          "description": "State is the current state of the instance."
        },
        "name": {
          "type": "string",
          "example": "ZITADEL",
          "description": "Name is the display name of the instance.\nThis can be changed by the instance administrator."
        },
        "version": {
          "type": "string",
          "example": "1.0.0",
          "description": "Version of the system the instance is running on.\nThis is managed by the system and cannot be changed by the instance administrator."
        },
        "customDomains": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2CustomDomain"
          },
          "description": "CustomDomains are the domains that are assigned to the instance.\nThe list includes auto-generated and manually added domains.\nThey are all unique across all instances in the system.\nThey're used to route requests to the correct instance."
        }
      }
    },
    "v2ListCustomDomainsResponse": {
      "type": "object",
      "properties": {
        "domains": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2CustomDomain"
          },
          "description": "The list of custom domains matching the query."
        },
        "pagination": {
          "$ref": "#/definitions/v2PaginationResponse",
          "description": "Contains the total number of domains matching the query and the applied limit."
        }
      }
    },
    "v2ListInstancesResponse": {
      "type": "object",
      "properties": {
        "instances": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2Instance"
          },
          "description": "The instances matching the query."
        },
        "pagination": {
          "$ref": "#/definitions/v2PaginationResponse",
          "description": "Contains the total number of instances matching the query and the applied limit."
        }
      }
    },
    "v2ListTrustedDomainsResponse": {
      "type": "object",
      "properties": {
        "trustedDomain": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v2TrustedDomain"
          },
          "description": "The list of trusted domains matching the query."
        },
        "pagination": {
          "$ref": "#/definitions/v2PaginationResponse",
          "description": "Contains the total number of domains matching the query and the applied limit."
        }
      }
    },
    "v2PaginationRequest": {
      "type": "object",
      "properties": {
        "offset": {
          "type": "string",
          "format": "uint64",
          "example": 0,
          "description": "Starting point for retrieval, in combination of offset used to query a set list of objects."
        },
        "limit": {
          "type": "integer",
          "format": "int64",
          "example": 10,
          "description": "limit is the maximum amount of objects returned. The default is set to 100\nwith a maximum of 1000 in the runtime configuration.\nIf the limit exceeds the maximum configured ZITADEL will throw an error.\nIf no limit is present the default is taken."
        },
        "asc": {
          "type": "boolean",
          "example": false,
          "description": "Asc is the sorting order. If true the list is sorted ascending, if false\nthe list is sorted descending. The default is descending."
        }
      }
    },
    "v2PaginationResponse": {
      "type": "object",
      "properties": {
        "totalResult": {
          "type": "string",
          "format": "uint64",
          "example": 100,
          "description": "Absolute number of objects matching the query, regardless of applied limit."
        },
        "appliedLimit": {
          "type": "string",
          "format": "uint64",
          "example": 100,
          "description": "Applied limit from query, defines maximum amount of objects per request, to compare if all objects are returned."
        }
      }
    },
    "v2RemoveCustomDomainResponse": {
      "type": "object",
      "properties": {
        "deletionDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "DeletionDate is the timestamp when the domain was removed."
        }
      }
    },
    "v2RemoveTrustedDomainResponse": {
      "type": "object",
      "properties": {
        "deletionDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "DeletionDate is the timestamp when the trusted domain was removed."
        }
      }
    },
    "v2State": {
      "type": "string",
      "enum": [
        "STATE_UNSPECIFIED",
        "STATE_CREATING",
        "STATE_RUNNING",
        "STATE_STOPPING",
        "STATE_STOPPED"
      ],
      "default": "STATE_UNSPECIFIED"
    },
    "v2TextQueryMethod": {
      "type": "string",
      "enum": [
        "TEXT_QUERY_METHOD_EQUALS",
        "TEXT_QUERY_METHOD_EQUALS_IGNORE_CASE",
        "TEXT_QUERY_METHOD_STARTS_WITH",
        "TEXT_QUERY_METHOD_STARTS_WITH_IGNORE_CASE",
        "TEXT_QUERY_METHOD_CONTAINS",
        "TEXT_QUERY_METHOD_CONTAINS_IGNORE_CASE",
        "TEXT_QUERY_METHOD_ENDS_WITH",
        "TEXT_QUERY_METHOD_ENDS_WITH_IGNORE_CASE"
      ],
      "default": "TEXT_QUERY_METHOD_EQUALS"
    },
    "v2TrustedDomain": {
      "type": "object",
      "properties": {
        "instanceId": {
          "type": "string",
          "description": "InstanceID is the unique identifier of the instance the domain belongs to."
        },
        "creationDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "The timestamp when the domain was created."
        },
        "domain": {
          "type": "string",
          "example": "zitadel.com",
          "description": "Domain is the fully qualified domain name."
        }
      }
    },
    "v2TrustedDomainFieldName": {
      "type": "string",
      "enum": [
        "TRUSTED_DOMAIN_FIELD_NAME_UNSPECIFIED",
        "TRUSTED_DOMAIN_FIELD_NAME_DOMAIN",
        "TRUSTED_DOMAIN_FIELD_NAME_CREATION_DATE"
      ],
      "default": "TRUSTED_DOMAIN_FIELD_NAME_UNSPECIFIED"
    },
    "v2TrustedDomainFilter": {
      "type": "object",
      "properties": {
        "domainFilter": {
          "$ref": "#/definitions/v2DomainFilter",
          "description": "Filter for a specific trusted domain."
        }
      }
    },
    "v2UpdateInstanceResponse": {
      "type": "object",
      "properties": {
        "changeDate": {
          "type": "string",
          "format": "date-time",
          "example": "2025-01-23T10:34:18.051Z",
          "description": "ChangeDate is the timestamp when the instance was last changed.\nIn case the instance was not changed during the call, the previous change date is returned."
        }
      }
    }
  }
}
